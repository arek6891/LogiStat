# =============================================================================
# LogiStat — Nginx reverse proxy
# Domena: logistat.logwin-logistics.com.pl   (UWAGA: TLD .com.pl, nie .com)
#
# STATUS: KONFIGURACJA ZAPASOWA — NIE JEST OBECNIE POTRZEBNA.
# Docelowy model publikacji (jak returns-hub) to firmowy reverse proxy IT
# pod adresem 10.15.12.67, ktory sam terminuje SSL i kieruje ruch na
# backend 10.153.1.31:5001 po zwyklym HTTP. W tym modelu nie potrzebujemy
# ani wlasnego Nginxa, ani wlasnego certyfikatu.  Patrz docs/IT_REQUEST.md.
# Ten plik zostawiamy na wypadek, gdyby IT kazalo nam terminowac SSL lokalnie.
# Po otrzymaniu certyfikatów: odkomentować blok HTTPS, zakomentować blok HTTP
#
# Przeładowanie po zmianie:
#   sudo nginx -t && sudo systemctl reload nginx
# =============================================================================

# ── TYMCZASOWY HTTP (do czasu otrzymania certyfikatów od IT) ─────────────────
server {
    listen 80;
    server_name logistat.logwin-logistics.com.pl;

    location / {
        proxy_pass         http://127.0.0.1:5001;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_read_timeout    120s;
        proxy_connect_timeout  10s;
        proxy_send_timeout    120s;
        client_max_body_size  50M;
    }

    access_log /var/log/nginx/logistat_access.log;
    error_log  /var/log/nginx/logistat_error.log;
}

# ── HTTPS (odkomentować gdy IT dostarczy certyfikaty) ────────────────────────
# Certyfikaty umieścić w:
#   /etc/nginx/ssl/logistat/cert.crt
#   /etc/nginx/ssl/logistat/cert.key
#
# server {
#     listen 80;
#     server_name logistat.logwin-logistics.com.pl;
#     return 301 https://$host$request_uri;
# }
#
# server {
#     listen 443 ssl;
#     server_name logistat.logwin-logistics.com.pl;
#
#     ssl_certificate     /etc/nginx/ssl/logistat/cert.crt;
#     ssl_certificate_key /etc/nginx/ssl/logistat/cert.key;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass         http://127.0.0.1:5001;
#         proxy_http_version 1.1;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#
#         proxy_read_timeout    120s;
#         proxy_connect_timeout  10s;
#         proxy_send_timeout    120s;
#         client_max_body_size  50M;
#     }
#
#     access_log /var/log/nginx/logistat_access.log;
#     error_log  /var/log/nginx/logistat_error.log;
# }
