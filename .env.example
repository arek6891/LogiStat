# Skopiuj do `.env` i uzupelnij. `.env` jest w .gitignore.
#
# Klucz sesji — BEZ NIEGO APLIKACJA NIE WSTANIE.
#   python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Dane bazy. Na dev mozna zostawic domyslne; na test/prod ustaw wlasne haslo.
POSTGRES_USER=logistat
POSTGRES_PASSWORD=logistat-dev
POSTGRES_DB=logistat
