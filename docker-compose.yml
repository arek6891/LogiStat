version: '3.8'

services:
  app:
    build: .
    container_name: logistat
    restart: always
    ports:
      - "5001:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=production
